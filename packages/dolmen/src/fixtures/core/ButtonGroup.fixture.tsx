import { Button, ButtonGroup } from '../../components';

function ButtonGroupDemo() {
  return (
    <ButtonGroup>
      <Button>One</Button>
      <Button>Two</Button>
      <Button>Three</Button>
      <Button>Four</Button>
    </ButtonGroup>
  );
}

export default () => <ButtonGroupDemo />;
